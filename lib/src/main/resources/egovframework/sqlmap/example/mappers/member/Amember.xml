<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="Amember">
	
	<!-- 리스트 -->
	<select id="mem_list" resultType="egovMap">
		<![CDATA[
			select 
				userid,
				level,
				name,
				birth,
				phone,
				email,
				addr1,
				addr2
			from lib_member m
			where userid like concat('%', #{sKey}, '%') 
			order by userid 
			limit #{recordCountPerPage} offset #{firstIndex}
		]]>
	</select>
	
	<!-- 총검색수 -->
	<select id="mem_count" resultType="int">
		<![CDATA[
			select count(*)
			from lib_member m
			where userid like concat('%', #{sKey}, '%') 
		]]>
	</select>
	
	<!-- 업데이트 -->
	<update id="mem_update">
		<![CDATA[
			update lib_member
			set 
			level = #{level},
			name = #{name},
			birth = #{birth},
			phone = #{phone},
			email = #{email},
			addr1 = #{addr1},
			addr2 = #{addr2}
			where userid  = #{userid}
		]]>
	</update>
	
	<delete id="mem_delete">
		<![CDATA[
			delete from lib_member
			where userid  = #{userid}
		]]>
	</delete>
	
</mapper>