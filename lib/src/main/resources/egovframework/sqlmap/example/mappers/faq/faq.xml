<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="faq">

	<!-- 리스트 -->
	<select id="faq_list" resultType="egovMap">
		<![CDATA[
			select 
				faq_id,
				userid,
				title,
				cnt,
				write_date
			from lib_faq f
			order by faq_id
			limit #{recordCountPerPage} offset #{firstIndex}
		]]>
	</select>
	
	<!-- 총검색수 -->
	<select id="faq_count" resultType="int">
		<![CDATA[
			select count(*)
			from lib_faq f
		]]>
	</select>
	
	<select id="faq_file_list" resultType="egovMap">
		<![CDATA[
			select 
			faq_file_id,
			faq_id,
			faq_file_name,
			faq_file_ori_name
			from lib_file_faq f
			where faq_id = #{faqId}
			order by faq_file_id 
		]]>
	</select>

</mapper>